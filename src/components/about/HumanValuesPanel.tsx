/**
 * Studio evidence helpers live inline on the About page now.
 * Kept so older imports do not break.
 */
export function StudioEvidence() {
  return null;
}

export const HumanValuesPanel = StudioEvidence;
